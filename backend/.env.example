# Backend (FastAPI)

# OpenAI API key (required for NLP/TTS utilities)
# OPENAI_API_KEY=your_openai_api_key_here

# CORS (comma-separated origins allowed to call the API)
# CORS_ORIGINS=http://localhost:3000
